# Railway Configuration
# https://railway.app

[variables]
APP_ENV = "prod"
PHP_VERSION = "8.4"

[services.database]
name = "postgres"
image = "postgres:16-alpine"

[services.database.variables]
POSTGRES_DB = "app"
POSTGRES_USER = "app"

[services.redis]
name = "redis"
image = "redis:7-alpine"
