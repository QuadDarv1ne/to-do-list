# Recurring Tasks & Time Tracking — Документация

## 📚 Обзор

Реализованы две важные функции для управления задачами:

1. **Повторяющиеся задачи (Recurring Tasks)** — автоматическое создание задач по расписанию
2. **Учёт времени (Time Tracking)** — отслеживание времени, затраченного на задачи

---

## 🔁 Повторяющиеся задачи

### Возможности

- ✅ Создание правил повторения для задач
- ✅ Поддержка частот: ежедневно, еженедельно, ежемесячно, ежегодно
- ✅ Настройка интервала (каждый N дней/недель/месяцев)
- ✅ Выбор конкретных дней недели (для еженедельных)
- ✅ Выбор конкретных дней месяца (для ежемесячных)
- ✅ Дата окончания повторений
- ✅ Автоматическая обработка через cron
- ✅ Статистика и мониторинг

### 📍 Маршруты

| Маршрут | Метод | Описание |
|---------|-------|----------|
| `/recurring/` | GET | Список правил повторения |
| `/recurring/new` | GET/POST | Создание правила |
| `/recurring/{id}` | GET | Просмотр правила |
| `/recurring/{id}/edit` | GET/POST | Редактирование |
| `/recurring/{id}` | POST | Удаление |
| `/recurring/{id}/toggle` | POST | Активировать/деактивировать |
| `/recurring/process` | POST | Обработка (admin) |

### 🔧 Команда для cron

```bash
# Обработка повторяющихся задач
php bin/console app:process-recurring-tasks

# Добавьте в crontab для ежедневного запуска в 00:00
0 0 * * * cd /path/to/project && php bin/console app:process-recurring-tasks
```

### 📊 Примеры использования

#### Ежедневная задача
```
Задача: "Отправить отчёт"
Частота: ежедневно
Интервал: 1
```
→ Задача создаётся каждый день

#### Еженедельная задача по понедельникам и пятницам
```
Задача: "Планерка"
Частота: еженедельно
Дни недели: понедельник (1), пятница (5)
```
→ Задача создаётся каждый понедельник и пятницу

#### Ежемесячная задача 15-го числа
```
Задача: "Оплата счетов"
Частота: ежемесячно
Дни месяца: 15
```
→ Задача создаётся 15-го числа каждого месяца

### 💻 API

```php
use App\Service\RecurringTaskService;

// Создание правила
$recurrence = $recurringService->createRecurring(
    $task,           // задача-шаблон
    $user,           // пользователь
    'weekly',        // частота
    1,               // интервал
    $endDate,        // дата окончания (опционально)
    [1, 3, 5],      // дни недели (опционально)
    [1, 15]         // дни месяца (опционально)
);

// Обработка (создание новых задач)
$createdTasks = $recurringService->processRecurringTasks();

// Получение правил пользователя
$recurrences = $recurringService->getUserRecurringTasks($user);

// Статистика
$stats = $recurringService->getStatistics($user);
```

---

## ⏱️ Учёт времени

### Возможности

- ✅ Запуск/остановка учёта времени для задач
- ✅ Отслеживание активных сессий
- ✅ История сессий
- ✅ Статистика по времени
- ✅ Группировка по дням и категориям
- ✅ Отчёты за период
- ✅ Продуктивность (score)
- ✅ Автоматическая остановка предыдущих сессий

### 📍 Маршруты

| Маршрут | Метод | Описание |
|---------|-------|----------|
| `/time-tracking/` | GET | Главная страница |
| `/time-tracking/start/{taskId}` | POST | Начать учёт |
| `/time-tracking/stop/{trackingId}` | POST | Остановить учёт |
| `/time-tracking/toggle/{taskId}` | POST | Переключить |
| `/time-tracking/session/{id}` | GET | Просмотр сессии |
| `/time-tracking/session/{id}/edit` | GET/POST | Редактирование |
| `/time-tracking/session/{id}` | POST | Удаление |
| `/time-tracking/report` | GET | Отчёт |
| `/time-tracking/api/active` | GET | Активная сессия (AJAX) |
| `/time-tracking/api/stop-all` | POST | Остановить всё (AJAX) |

### 💻 API

```php
use App\Service\TimeTrackingService;

// Начать учёт времени
$tracking = $timeTrackingService->startTracking($task, $user, 'Описание работы');

// Остановить учёт
$tracking = $timeTrackingService->stopTracking($tracking);

// Остановить по ID
$tracking = $timeTrackingService->stopTrackingById($trackingId);

// Переключить (старт/стоп)
$result = $timeTrackingService->toggleTracking($task, $user);

// Получить активную сессию
$activeSession = $timeTrackingService->getActiveSession($user);

// Получить время, затраченное на задачу (секунды)
$totalSeconds = $timeTrackingService->getTimeSpent($task);

// Статистика за период
$stats = $timeTrackingService->getStatistics($user, $from, $to);

// Отчёт
$report = $timeTrackingService->getReport($user, $from, $to);

// Продуктивность (0-100)
$score = $timeTrackingService->getProductivityScore($user, 7);

// Форматирование длительности
$formatted = $timeTrackingService->formatDuration(3665); // "1ч 1м 5с"
```

### 📊 Структура данных TaskTimeTracking

```php
class TaskTimeTracking {
    int $id;
    User $user;
    Task $task;
    ?string $description;
    ?DateTime $startedAt;
    ?DateTime $endedAt;
    int $durationSeconds;
    bool $isActive;
    DateTime $dateLogged;
}
```

### 🎯 Формула продуктивности

Продуктивность рассчитывается на основе:

1. **Общее время** (макс. 40 баллов)
   - Ожидается: 8 часов в день (28800 секунд)
   
2. **Количество задач** (макс. 40 баллов)
   - Ожидается: 5 задач в день
   
3. **Консистентность** (макс. 20 баллов)
   - Дни с учётом времени / общее количество дней

---

## 🔧 Интеграция в существующий код

### Добавление кнопки старта/стопа в TaskController

```php
// В TaskController::show()
$activeSession = $timeTrackingService->getActiveSessionForTask($task, $user);
$currentTime = $activeSession ? $activeSession->calculateDuration() : 0;
$totalTime = $timeTrackingService->getTimeSpent($task);

return $this->render('task/show.html.twig', [
    'task' => $task,
    'activeSession' => $activeSession,
    'currentTime' => $currentTime,
    'totalTime' => $totalTime,
]);
```

### Twig шаблон (кнопка)

```twig
{# В task/show.html.twig #}
<div class="time-tracking-section">
    <h4>Учёт времени</h4>
    
    {% if activeSession %}
        <div class="alert alert-success">
            <i class="fas fa-clock"></i>
            Учёт времени активен
            <span id="timer">{{ currentTime|duration_format }}</span>
        </div>
        
        <form method="post" action="{{ path('app_time_tracking_stop', {trackingId: activeSession.id}) }}">
            <button class="btn btn-danger">
                <i class="fas fa-stop"></i> Остановить
            </button>
        </form>
    {% else %}
        <form method="post" action="{{ path('app_time_tracking_start', {taskId: task.id}) }}">
            <button class="btn btn-primary">
                <i class="fas fa-play"></i> Начать учёт
            </button>
        </form>
    {% endif %}
    
    <p class="text-muted">
        Всего затрачено: {{ totalTime|duration_format }}
    </p>
</div>
```

---

## 📝 Планы развития

### Recurring Tasks

- [ ] Поддержка сложных паттернов (каждый второй понедельник)
- [ ] Пропуск выходных дней
- [ ] Уведомления о создании задач
- [ ] Шаблонные правила для всех пользователей
- [ ] Календарь предстоящих повторений

### Time Tracking

- [ ] Pomodoro таймер
- [ ] Цели по времени на задачу
- [ ] Экспорт отчётов в PDF/Excel
- [ ] Интеграция с календарём
- [ ] Мобильное приложение для трекинга
- [ ] Real-time обновление таймера (WebSocket)

---

## ⚠️ Важные замечания

1. **Cron:** Для автоматической обработки повторяющихся задач настройте cron
2. **Очистка:** Старые записи учёта времени рекомендуется удалять (>90 дней)
3. **Производительность:** При большом количестве задач используйте индексацию
4. **Безопасность:** Пользователи видят только свои записи

---

## 📖 Связанные документы

- [Задачи](docs/TASKS.md)
- [Пользователи](docs/USERS.md)
- [Отчёты](docs/REPORTS.md)
- [API документация](docs/API.md)
