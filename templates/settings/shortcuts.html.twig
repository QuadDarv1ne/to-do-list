{% extends 'base.html.twig' %}

{% block title %}Горячие клавиши{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="fas fa-keyboard me-2"></i> Горячие клавиши</h1>
        <a href="{{ path('app_settings') }}" class="btn btn-outline-secondary-modern">
            <i class="fas fa-arrow-left me-2"></i> Назад к настройкам
        </a>
    </div>

    <div class="alert alert-info border-0 shadow-sm">
        <i class="fas fa-info-circle me-2"></i>
        Используйте горячие клавиши для быстрого доступа к функциям системы
    </div>

    <div class="row">
        <div class="col-md-4 mb-4">
            <div class="card-modern">
                <div class="card-header-modern bg-primary text-white">
                    <h5 class="mb-0"><i class="fas fa-globe me-2"></i> Глобальные</h5>
                </div>
                <div class="list-group list-group-flush">
                    {% for shortcut in shortcuts.global %}
                    <div class="list-group-item border-0 py-3">
                        <div class="d-flex justify-content-between align-items-center">
                            <span>{{ shortcut.description }}</span>
                            <kbd class="bg-dark text-white px-2 py-1 rounded">{{ shortcut.keys }}</kbd>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <div class="col-md-4 mb-4">
            <div class="card-modern">
                <div class="card-header-modern bg-success text-white">
                    <h5 class="mb-0"><i class="fas fa-tasks me-2"></i> Задачи</h5>
                </div>
                <div class="list-group list-group-flush">
                    {% for shortcut in shortcuts.tasks %}
                    <div class="list-group-item border-0 py-3">
                        <div class="d-flex justify-content-between align-items-center">
                            <span>{{ shortcut.description }}</span>
                            <kbd class="bg-dark text-white px-2 py-1 rounded">{{ shortcut.keys }}</kbd>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <div class="col-md-4 mb-4">
            <div class="card-modern">
                <div class="card-header-modern bg-info text-white">
                    <h5 class="mb-0"><i class="fas fa-compass me-2"></i> Навигация</h5>
                </div>
                <div class="list-group list-group-flush">
                    {% for shortcut in shortcuts.navigation %}
                    <div class="list-group-item border-0 py-3">
                        <div class="d-flex justify-content-between align-items-center">
                            <span>{{ shortcut.description }}</span>
                            <kbd class="bg-dark text-white px-2 py-1 rounded">{{ shortcut.keys }}</kbd>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

    <div class="card-modern mt-4">
        <div class="card-header-modern">
            <h5 class="mb-0"><i class="fas fa-question-circle me-2"></i> Как использовать</h5>
        </div>
        <div class="card-body-modern">
            <ul>
                <li><strong>Ctrl + K</strong> - Откроет быстрый поиск в любом месте системы</li>
                <li><strong>Ctrl + N</strong> - Создаст новую задачу без перехода на страницу создания</li>
                <li><strong>G + D</strong> - Нажмите "G", затем "D" для перехода к дашборду</li>
                <li><strong>Esc</strong> - Закроет любое открытое модальное окно</li>
            </ul>
            
            <div class="alert alert-warning border-0 shadow-sm mt-3">
                <i class="fas fa-exclamation-triangle me-2"></i>
                <strong>Примечание:</strong> Некоторые горячие клавиши могут не работать в полях ввода текста
            </div>
        </div>
    </div>
</div>

<style>
kbd {
    font-family: 'Courier New', monospace;
    font-size: 0.9em;
    font-weight: bold;
}
.list-group-item {
    padding: 0.75rem 1rem;
}
</style>
{% endblock %}