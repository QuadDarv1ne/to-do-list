<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}CRM система: Анализ продаж{% endblock %}</title>
    
    <!-- Content Security Policy -->
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com; img-src 'self' data: https:; font-src 'self' https://cdnjs.cloudflare.com; connect-src 'self'; frame-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; upgrade-insecure-requests;">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    
    <!-- PWA Manifest -->
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#0d6efd">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-title" content="To-Do List">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    
    <!-- CSRF Token -->
    <meta name="csrf-token" content="{{ csrf_token('authenticate') }}">
    
    <!-- Resource Hints -->
    <link rel="dns-prefetch" href="https://cdn.jsdelivr.net">
    <link rel="dns-prefetch" href="https://cdnjs.cloudflare.com">
    <link rel="preconnect" href="https://cdn.jsdelivr.net">
    <link rel="preconnect" href="https://cdnjs.cloudflare.com">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <!-- Critical CSS - Inline for instant rendering -->
    <style>
        {% include 'inline-critical.css' ignore missing %}
    </style>
    
    {% block stylesheets %}
        <!-- Core styles - Load immediately -->
        <link rel="stylesheet" href="/css/main.css">
        <link rel="stylesheet" href="/css/utilities.css">
        <link rel="stylesheet" href="/css/components.css">
        <link rel="stylesheet" href="/css/themes.css">
        <link rel="stylesheet" href="/css/dark-modern-theme.css">
        <link rel="stylesheet" href="/css/animations.css">
        <link rel="stylesheet" href="/css/pages.css">
        <link rel="stylesheet" href="/css/modern-interactions.css">
        <link rel="stylesheet" href="/css/micro-animations.css">
        <link rel="stylesheet" href="/css/responsive-enhancements.css">
        <link rel="stylesheet" href="/css/accessibility.css">
        <link rel="stylesheet" href="/css/performance-optimizations.css">
        <link rel="stylesheet" href="/css/footer-enhanced.css">
        <link rel="stylesheet" href="/css/card-system.css">
        <link rel="stylesheet" href="/css/button-system.css">
        <link rel="stylesheet" href="/css/form-system.css">
        <link rel="stylesheet" href="/css/typography-system.css">
        <link rel="stylesheet" href="/css/modal-system.css">
        <link rel="stylesheet" href="/css/table-system.css">
        <link rel="stylesheet" href="/css/navigation-system.css">
        <link rel="stylesheet" href="/css/layout-system.css">
        
        <!-- Progressive enhancement - Load async -->
        <link rel="preload" href="/css/gradient-effects.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
        <link rel="preload" href="/css/advanced-components.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
        <link rel="preload" href="/css/theme-transitions.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
        <noscript>
            <link rel="stylesheet" href="/css/gradient-effects.css">
            <link rel="stylesheet" href="/css/advanced-components.css">
            <link rel="stylesheet" href="/css/theme-transitions.css">
        </noscript>
    {% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-enhanced">
        <div class="container-fluid">
            <a class="navbar-brand-enhanced" href="{{ path('app_dashboard') }}">
                <div class="brand-icon">
                    <i class="fas fa-chart-line"></i>
                </div>
                <div class="brand-text d-none d-sm-block">
                    <div class="brand-title">CRM</div>
                    <div class="brand-subtitle">Task Management</div>
                </div>
            </a>
            
            <button class="navbar-toggler navbar-toggler-enhanced" type="button" 
                    data-bs-toggle="collapse" 
                    data-bs-target="#navbarNav"
                    aria-controls="navbarNav"
                    aria-expanded="false"
                    aria-label="Переключить навигацию">
                <span class="navbar-toggler-icon navbar-toggler-icon-enhanced"></span>
            </button>
            
            <div class="collapse navbar-collapse navbar-collapse-enhanced" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link nav-link-enhanced" href="{{ path('app_dashboard') }}">
                                <i class="fas fa-home"></i>
                                <span>Панель</span>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link nav-link-enhanced" href="{{ path('app_task_index') }}">
                                <i class="fas fa-tasks"></i>
                                <span>Задачи</span>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link nav-link-enhanced" href="{{ path('app_kanban_board') }}">
                                <i class="fas fa-columns"></i>
                                <span class="d-none d-lg-inline">Канбан</span>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link nav-link-enhanced" href="{{ path('app_calendar') }}">
                                <i class="fas fa-calendar"></i>
                                <span class="d-none d-lg-inline">Календарь</span>
                            </a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link nav-link-enhanced dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-briefcase"></i>
                                <span>CRM</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-enhanced">
                                <li><a class="dropdown-item dropdown-item-enhanced" href="{{ path('app_deals_index') }}">
                                    <i class="fas fa-handshake"></i>Сделки
                                </a></li>
                                <li><a class="dropdown-item dropdown-item-enhanced" href="{{ path('app_clients_index') }}">
                                    <i class="fas fa-users"></i>Клиенты
                                </a></li>
                            </ul>
                        </li>
                        <li class="nav-item dropdown d-none d-xl-block">
                            <a class="nav-link nav-link-enhanced dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-chart-line"></i>
                                <span>Аналитика</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-enhanced">
                                <li><a class="dropdown-item dropdown-item-enhanced" href="{{ path('app_analytics_dashboard') }}">
                                    <i class="fas fa-chart-pie"></i>Дашборд
                                </a></li>
                                <li><a class="dropdown-item dropdown-item-enhanced" href="{{ path('app_advanced_analytics') }}">
                                    <i class="fas fa-chart-area"></i>Расширенная
                                </a></li>
                                <li><a class="dropdown-item dropdown-item-enhanced" href="{{ path('app_sales_analytics') }}">
                                    <i class="fas fa-dollar-sign"></i>Продажи
                                </a></li>
                            </ul>
                        </li>
                        {% if app.user.hasRole('ROLE_ADMIN') %}
                            <li class="nav-item d-none d-xl-block">
                                <a class="nav-link nav-link-enhanced" href="{{ path('app_user_index') }}">
                                    <i class="fas fa-users-cog"></i>
                                    <span>Пользователи</span>
                                </a>
                            </li>
                        {% endif %}
                    {% endif %}
                </ul>
                
                {% if app.user %}
                <ul class="navbar-nav ms-auto align-items-center">
                    <!-- Quick Actions -->
                    <li class="nav-item d-none d-lg-block me-2">
                        <a class="quick-action-btn-enhanced" href="{{ path('app_task_new') }}" title="Новая задача">
                            <i class="fas fa-plus"></i>
                        </a>
                    </li>
                    
                    <!-- Notifications -->
                    <li class="nav-item dropdown me-2">
                        <a class="quick-action-btn-enhanced position-relative" 
                           href="#" 
                           role="button" 
                           data-bs-toggle="dropdown"
                           aria-expanded="false"
                           aria-label="Уведомления">
                            <i class="fas fa-bell"></i>
                            <span class="notification-badge-enhanced">3</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-enhanced dropdown-menu-end" style="min-width: 300px;">
                            <li>
                                <h6 class="dropdown-header dropdown-header-enhanced">
                                    Уведомления
                                </h6>
                            </li>
                            <li><hr class="dropdown-divider dropdown-divider-enhanced"></li>
                            <li>
                                <a class="dropdown-item dropdown-item-enhanced" href="#">
                                    <i class="fas fa-info-circle"></i>
                                    <div>
                                        <div class="fw-semibold">Новая задача назначена</div>
                                        <small class="text-muted">5 минут назад</small>
                                    </div>
                                </a>
                            </li>
                            <li><hr class="dropdown-divider dropdown-divider-enhanced"></li>
                            <li>
                                <a class="dropdown-item dropdown-item-enhanced text-center" href="{{ path('app_notification_index') }}">
                                    Все уведомления
                                </a>
                            </li>
                        </ul>
                    </li>
                    
                    <!-- User Dropdown -->
                    <li class="nav-item dropdown">
                        <a class="nav-link p-0" 
                           href="#" 
                           role="button" 
                           data-bs-toggle="dropdown"
                           aria-expanded="false"
                           aria-label="Меню пользователя">
                            <div class="user-avatar-enhanced">
                                {{ app.user.firstName|first }}{{ app.user.lastName|first }}
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-enhanced dropdown-menu-end">
                            <li>
                                <div class="dropdown-header dropdown-header-enhanced">
                                    <strong>{{ app.user.fullName }}</strong>
                                    <div class="small text-muted">{{ app.user.email }}</div>
                                </div>
                            </li>
                            <li><hr class="dropdown-divider dropdown-divider-enhanced"></li>
                            <li>
                                <a class="dropdown-item dropdown-item-enhanced" href="{{ path('app_profile_show') }}">
                                    <i class="fas fa-user"></i>Мой профиль
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item dropdown-item-enhanced" href="{{ path('app_settings') }}">
                                    <i class="fas fa-cog"></i>Настройки
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item dropdown-item-enhanced" href="{{ path('app_2fa_setup') }}">
                                    <i class="fas fa-shield-alt"></i>Безопасность
                                </a>
                            </li>
                            <li><hr class="dropdown-divider dropdown-divider-enhanced"></li>
                            <li>
                                <a class="dropdown-item dropdown-item-enhanced text-danger" href="#" data-bs-toggle="modal" data-bs-target="#logoutModal">
                                    <i class="fas fa-sign-out-alt"></i>Выход
                                </a>
                            </li>
                        </ul>
                    </li>
                </ul>
                {% else %}
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link nav-link-enhanced" href="{{ path('app_login') }}">
                            <i class="fas fa-sign-in-alt"></i>Вход
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link nav-link-enhanced" href="{{ path('app_register') }}">
                            <i class="fas fa-user-plus"></i>Регистрация
                        </a>
                    </li>
                </ul>
                {% endif %}
            </div>
        </div>
    </nav>
    
    
    <main class="container-fluid mt-4">
        {% for flash_message in app.session.flashbag.get('success') %}
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                {{ flash_message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
        
        {% for flash_message in app.session.flashbag.get('error') %}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                {{ flash_message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
        
        {% block body %}{% endblock %}
    </main>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" defer></script>
    
    <!-- Service Worker Registration -->
    <script>
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', function() {
                navigator.serviceWorker.register('/sw.js')
                    .then(function(registration) {
                        console.log('SW registered: ', registration);
                    })
                    .catch(function(registrationError) {
                        console.log('SW registration failed: ', registrationError);
                    });
            });
        }
    </script>
    
    <!-- Toast Container -->
    <div class="toast-container"></div>
    
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
        
        {# Critical - Load immediately #}
        <script src="/js/state-manager.js"></script>
        <script src="/js/font-loader.js"></script>
        
        {# Core utilities #}
        <script src="/js/utils.js" defer></script>
        <script src="/js/performance-optimizer.js" defer></script>
        <script src="/js/css-loader.js" defer></script>
        
        {# Critical functionality #}
        <script src="/js/notification-system.js" defer></script>
        <script src="/js/theme-switcher.js" defer></script>
        <script src="/js/offline-support.js" defer></script>
        <script src="/js/form-utilities.js" defer></script>
        <script src="/js/global-interactions.js" defer></script>
        
        {# UI Enhancements #}
        <script src="/js/ui-enhancements.js" defer></script>
        <script src="/js/modern-interactions.js" defer></script>
        <script src="/js/component-manager.js" defer></script>
        <script src="/js/hotkeys.js" defer></script>
        <script src="/js/quick-search.js" defer></script>
        <script src="/js/keyboard-shortcuts.js" defer></script>
        
        {# Feature modules - lazy loaded #}
        <script src="/js/lazy-load.js" defer></script>
        <script src="/js/auto-save.js" defer></script>
        <script src="/js/drag-drop-manager.js" defer></script>
        <script src="/js/image-optimizer.js" defer></script>
        <script src="/js/performance-monitor.js" defer></script>
        <script>
            // Preload important resources
            if ('requestIdleCallback' in window) {
                requestIdleCallback(function() {
                    // Lazy load non-critical functionality
                    loadNonCriticalFeatures();
                });
            } else {
                setTimeout(function() {
                    loadNonCriticalFeatures();
                }, 1000);
            }
            
            function loadNonCriticalFeatures() {
                // Feature-detect and load polyfills only when needed
                if (!('IntersectionObserver' in window)) {
                    // Load intersection observer polyfill
                    loadScript('https://polyfill.io/v3/polyfill.min.js?features=IntersectionObserver');
                }
            }
            
            function loadScript(src) {
                const script = document.createElement('script');
                script.src = src;
                document.head.appendChild(script);
            }
        </script>
    {% endblock %}
    
    <!-- Real-time Notifications JavaScript -->
    {% if app.user %}
    <script>
        class RealTimeNotifications {
            constructor() {
                this.eventSource = null;
                this.notificationCountElement = document.getElementById('notificationCount');
                this.notificationsMenu = document.getElementById('notificationsMenu');
                this.markAllReadBtn = document.getElementById('markAllRead');
                this.toastContainer = document.getElementById('notificationToastContainer');
                
                this.init();
            }
            
            init() {
                this.connect();
                this.bindEvents();
                this.updateNotificationCount();
            }
            
            connect() {
                if (!window.EventSource) {
                    console.warn('EventSource not supported');
                    return;
                }
                
                // EventSource disabled to reduce server load
                // Use polling instead
                this.startPolling();
            }
            
            startPolling() {
                // Poll for notifications every 2 minutes instead of constant stream
                this.updateNotificationCount();
                setInterval(() => {
                    this.updateNotificationCount();
                }, 120000); // 2 minutes
            }
            
            handleNotification(notification) {
                // Update notification count
                this.updateNotificationCount();
                
                // Show toast notification
                this.showNotificationToast(notification);
                
                // Add to dropdown menu
                this.addNotificationToMenu(notification);
                
                // Play sound if enabled
                this.playNotificationSound();
            }
            
            showNotificationToast(notification) {
                const toastHtml = `
                    <div class="toast" role="alert" aria-live="assertive" aria-atomic="true">
                        <div class="toast-header">
                            <strong class="me-auto">${notification.title}</strong>
                            <small class="text-muted">только что</small>
                            <button type="button" class="btn-close" data-bs-dismiss="toast"></button>
                        </div>
                        <div class="toast-body">
                            ${notification.message}
                        </div>
                    </div>
                `;
                
                this.toastContainer.insertAdjacentHTML('beforeend', toastHtml);
                const toastElement = this.toastContainer.lastElementChild;
                const toast = new bootstrap.Toast(toastElement);
                toast.show();
                
                // Auto-remove after showing
                toastElement.addEventListener('hidden.bs.toast', () => {
                    toastElement.remove();
                });
            }
            
            addNotificationToMenu(notification) {
                const menuItem = document.createElement('li');
                menuItem.innerHTML = `
                    <a class="dropdown-item d-flex flex-column fw-bold" href="#">
                        <div class="d-flex justify-content-between">
                            <span>${notification.title}</span>
                            <small class="text-muted">только что</small>
                        </div>
                        <div class="text-muted small">${notification.message}</div>
                    </a>
                `;
                
                // Insert after header but before existing notifications
                const divider = this.notificationsMenu.querySelector('.dropdown-divider');
                this.notificationsMenu.insertBefore(menuItem, divider);
            }
            
            async updateNotificationCount() {
                try {
                    const response = await fetch('/api/notifications/unread-count');
                    const data = await response.json();
                    
                    if (this.notificationCountElement) {
                        this.notificationCountElement.textContent = data.unread;
                        this.notificationCountElement.classList.toggle('d-none', data.unread === 0);
                    }
                } catch (error) {
                    console.error('Failed to update notification count:', error);
                }
            }
            
            bindEvents() {
                if (this.markAllReadBtn) {
                    this.markAllReadBtn.addEventListener('click', async (e) => {
                        e.preventDefault();
                        await this.markAllAsRead();
                    });
                }
            }
            
            async markAllAsRead() {
                try {
                    const response = await fetch('/api/notifications/mark-all-read', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        }
                    });
                    
                    if (response.ok) {
                        await this.updateNotificationCount();
                        // Clear dropdown notifications
                        const notificationItems = this.notificationsMenu.querySelectorAll('.dropdown-item.fw-bold');
                        notificationItems.forEach(item => item.classList.remove('fw-bold'));
                    }
                } catch (error) {
                    console.error('Failed to mark notifications as read:', error);
                }
            }
            
            playNotificationSound() {
                // Optional: play notification sound
                // const audio = new Audio('/sounds/notification.mp3');
                // audio.play().catch(e => console.log('Audio play failed:', e));
            }
            
            disconnect() {
                if (this.eventSource) {
                    this.eventSource.close();
                }
            }
        }
        
        // Initialize real-time notifications
        document.addEventListener('DOMContentLoaded', () => {
            window.realTimeNotifications = new RealTimeNotifications();
        });
    </script>
    {% endif %}
    
    {% if app.user %}
    <!-- Quick Task Modal -->
    <div class="modal fade" id="quickTaskModal" tabindex="-1" aria-labelledby="quickTaskModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="quickTaskModalLabel">Быстрое создание задачи</h5>
                    <button type="button" class="btn-close btn-enhanced" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="quick-task-form">
                        <div class="mb-3">
                            <label for="quick-task-title" class="form-label">Название задачи *</label>
                            <input type="text" class="form-control" id="quick-task-title" required placeholder="Введите название задачи">
                        </div>
                        <div class="mb-3">
                            <label for="quick-task-description" class="form-label">Описание</label>
                            <textarea class="form-control" id="quick-task-description" rows="3" placeholder="Добавьте описание задачи"></textarea>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="quick-task-priority" class="form-label">Приоритет</label>
                                <select class="form-select" id="quick-task-priority">
                                    <option value="low">Низкий</option>
                                    <option value="medium" selected>Средний</option>
                                    <option value="high">Высокий</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="quick-task-due-date" class="form-label">Срок выполнения</label>
                                <input type="date" class="form-control" id="quick-task-due-date">
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="quick-task-category" class="form-label">Категория</label>
                            <select class="form-select" id="quick-task-category">
                                <option value="">Без категории</option>
                                {% if app.user %}
                                    {% for category in app.user.taskCategories %}
                                        <option value="{{ category.id }}">{{ category.name }}</option>
                                    {% endfor %}
                                {% endif %}
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="quick-task-assigned-user" class="form-label">Назначить пользователю</label>
                            <select class="form-select" id="quick-task-assigned-user">
                                <option value="">Назначить себе</option>
                                {% if app.user %}
                                    <option value="{{ app.user.id }}">{{ app.user.fullName }}</option>
                                    <!-- This will be populated dynamically via JavaScript -->
                                {% endif %}
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="quick-task-tags" class="form-label">Теги</label>
                            <select class="form-select" id="quick-task-tags" multiple>
                                <!-- This will be populated dynamically via JavaScript -->
                            </select>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary btn-enhanced" data-bs-dismiss="modal">Отмена</button>
                    <button type="button" class="btn btn-primary btn-enhanced" id="save-quick-task">Создать задачу</button>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Floating Quick Task Button -->
    <button type="button" 
            class="btn btn-primary btn-lg btn-enhanced rounded-circle shadow position-fixed fab-enter" 
            style="bottom: 30px; right: 30px; z-index: 1000; width: 60px; height: 60px;" 
            id="quick-task-fab" 
            data-bs-toggle="modal" 
            data-bs-target="#quickTaskModal"
            aria-label="Быстрое создание задачи"
            title="Быстрое создание задачи (N)">
        <i class="fas fa-plus"></i>
    </button>
    {% endif %}
    
    <script>
        // PWA Installation
        let deferredPrompt;
        
        window.addEventListener('beforeinstallprompt', (e) => {
            // Prevent Chrome 67 and earlier from automatically showing the prompt
            e.preventDefault();
            // Stash the event so it can be triggered later
            deferredPrompt = e;
            
            // Show the install prompt after a delay
            setTimeout(() => {
                const installPrompt = document.getElementById('installPrompt');
                if (installPrompt && !localStorage.getItem('installPromptDismissed')) {
                    installPrompt.classList.add('show');
                }
            }, 3000);
        });
        
        // Installation button handler
        document.addEventListener('DOMContentLoaded', function() {
            const installButton = document.getElementById('installButton');
            const dismissButton = document.getElementById('dismissInstall');
            const installPrompt = document.getElementById('installPrompt');
            
            if (installButton) {
                installButton.addEventListener('click', async () => {
                    if (!deferredPrompt) return;
                    
                    // Show the install prompt
                    deferredPrompt.prompt();
                    
                    // Wait for the user to respond to the prompt
                    const { outcome } = await deferredPrompt.userChoice;
                    
                    if (outcome === 'accepted') {
                        showToast('Приложение установлено!', 'success');
                    }
                    
                    // We've used the prompt, and can't use it again, throw it away
                    deferredPrompt = null;
                    installPrompt.classList.remove('show');
                });
            }
            
            if (dismissButton) {
                dismissButton.addEventListener('click', () => {
                    installPrompt.classList.remove('show');
                    localStorage.setItem('installPromptDismissed', 'true');
                });
            }
        });
        
        document.addEventListener('DOMContentLoaded', function() {
            // Theme switching is now handled by theme-switcher.js
            
            // Keyboard Shortcuts
            const shortcuts = {
                'n': () => {
                    if (document.getElementById('quick-task-fab')) {
                        document.getElementById('quick-task-fab').click();
                    }
                },
                't': () => window.location.href = '{{ path('app_task_index') }}',
                'd': () => window.location.href = '{{ path('app_dashboard') }}',
                'c': () => window.location.href = '{{ path('app_task_category_index') }}',
                'p': () => window.location.href = '{{ path('app_profile_show') }}',
                '?': () => showKeyboardShortcuts()
            };
            
            document.addEventListener('keydown', function(e) {
                // Ignore if typing in input/textarea
                if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') return;
                
                const key = e.key.toLowerCase();
                if (shortcuts[key] && (e.ctrlKey || !Object.keys(shortcuts).includes(key))) {
                    e.preventDefault();
                    shortcuts[key]();
                }
            });
            
            // Show shortcuts modal
            const showShortcutsBtn = document.getElementById('showShortcuts');
            if (showShortcutsBtn) {
                showShortcutsBtn.addEventListener('click', showKeyboardShortcuts);
            }
            
            function showKeyboardShortcuts() {
                const modalHtml = `
                    <div class="modal fade" id="shortcutsModal" tabindex="-1">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title"><i class="fas fa-keyboard me-2"></i>Горячие клавиши</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                </div>
                                <div class="modal-body">
                                    <div class="row">
                                        <div class="col-md-6">
                                            <h6>Навигация</h6>
                                            <ul class="list-unstyled">
                                                <li class="mb-2"><kbd class="kbd-shortcut">d</kbd> Панель управления</li>
                                                <li class="mb-2"><kbd class="kbd-shortcut">t</kbd> Задачи</li>
                                                <li class="mb-2"><kbd class="kbd-shortcut">c</kbd> Категории</li>
                                                <li class="mb-2"><kbd class="kbd-shortcut">p</kbd> Профиль</li>
                                            </ul>
                                        </div>
                                        <div class="col-md-6">
                                            <h6>Действия</h6>
                                            <ul class="list-unstyled">
                                                <li class="mb-2"><kbd class="kbd-shortcut">n</kbd> Быстрое создание задачи</li>
                                                <li class="mb-2"><kbd class="kbd-shortcut">?</kbd> Показать горячие клавиши</li>
                                                <li class="mb-2"><kbd class="kbd-shortcut">Esc</kbd> Закрыть модальные окна</li>
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Закрыть</button>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
                
                // Remove existing modal if present
                const existingModal = document.getElementById('shortcutsModal');
                if (existingModal) existingModal.remove();
                
                document.body.insertAdjacentHTML('beforeend', modalHtml);
                const modal = new bootstrap.Modal(document.getElementById('shortcutsModal'));
                modal.show();
                
                // Clean up after modal is hidden
                document.getElementById('shortcutsModal').addEventListener('hidden.bs.modal', function() {
                    this.remove();
                });
            }
            
            // Toast notifications
            window.showToast = function(message, type = 'info') {
                const toastContainer = document.querySelector('.toast-container');
                const toastId = 'toast-' + Date.now();
                
                const toastHtml = `
                    <div id="${toastId}" class="toast toast-${type} fade-in" role="alert" aria-live="assertive" aria-atomic="true">
                        <div class="toast-body">
                            <div class="d-flex align-items-center">
                                <i class="fas fa-${type === 'success' ? 'check-circle' : type === 'error' ? 'exclamation-circle' : type === 'warning' ? 'exclamation-triangle' : 'info-circle'} me-2"></i>
                                <span>${message}</span>
                                <button type="button" class="btn-close ms-auto" data-bs-dismiss="toast" aria-label="Закрыть уведомление"></button>
                            </div>
                        </div>
                    </div>
                `;
                
                toastContainer.insertAdjacentHTML('beforeend', toastHtml);
                const toastEl = document.getElementById(toastId);
                const toast = new bootstrap.Toast(toastEl, { delay: 3000 });
                toast.show();
                
                // Remove toast after it's hidden
                toastEl.addEventListener('hidden.bs.toast', function() {
                    this.remove();
                });
            };
            
            // Navbar scroll effect
            let lastScrollTop = 0;
            const navbar = document.querySelector('.navbar');
            
            window.addEventListener('scroll', function() {
                const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
                
                if (scrollTop > 50) {
                    navbar.classList.add('scrolled');
                } else {
                    navbar.classList.remove('scrolled');
                }
                
                lastScrollTop = scrollTop;
            });
            
            // Focus management for modals
            document.addEventListener('shown.bs.modal', function(e) {
                const modal = e.target;
                const firstFocusable = modal.querySelector('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])');
                if (firstFocusable) {
                    firstFocusable.focus();
                }
            });
            
            // Enhanced keyboard navigation
            document.addEventListener('keydown', function(e) {
                // ESC to close modals
                if (e.key === 'Escape') {
                    const modals = document.querySelectorAll('.modal.show');
                    modals.forEach(modal => {
                        const bsModal = bootstrap.Modal.getInstance(modal);
                        if (bsModal) bsModal.hide();
                    });
                }
                
                // Tab navigation improvements
                if (e.key === 'Tab') {
                    const activeModal = document.querySelector('.modal.show');
                    if (activeModal) {
                        const focusableElements = activeModal.querySelectorAll(
                            'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
                        );
                        const firstElement = focusableElements[0];
                        const lastElement = focusableElements[focusableElements.length - 1];
                        
                        if (e.shiftKey && document.activeElement === firstElement) {
                            lastElement.focus();
                            e.preventDefault();
                        } else if (!e.shiftKey && document.activeElement === lastElement) {
                            firstElement.focus();
                            e.preventDefault();
                        }
                    }
                }
            });
            // Quick Task functionality
            const quickTaskFab = document.getElementById('quick-task-fab');
            const saveQuickTaskBtn = document.getElementById('save-quick-task');
            const quickTaskForm = document.getElementById('quick-task-form');
            
            // Function to populate dropdowns when modal is shown
            const quickTaskModal = document.getElementById('quickTaskModal');
            quickTaskModal.addEventListener('shown.bs.modal', function() {
                // Populate categories dropdown
                const categorySelect = document.getElementById('quick-task-category');
                // Clear existing options except the first one
                while (categorySelect.options.length > 1) {
                    categorySelect.remove(1);
                }
                
                fetch('{{ path('app_api_categories') }}')
                    .then(resp => resp.json())
                    .then(categories => {
                        categories.forEach(category => {
                            const option = document.createElement('option');
                            option.value = category.id;
                            option.textContent = category.name;
                            categorySelect.appendChild(option);
                        });
                    })
                    .catch(error => {
                        console.error('Error loading categories:', error);
                    });
                
                // Populate users dropdown
                const userSelect = document.getElementById('quick-task-assigned-user');
                // Clear existing options except the first one
                while (userSelect.options.length > 1) {
                    userSelect.remove(1);
                }
                
                // Add current user option (if user is authenticated)
                {% if app.user %}
                const currentUserOption = document.createElement('option');
                currentUserOption.value = '{{ app.user.id }}';
                currentUserOption.textContent = '{{ app.user.fullName }}';
                userSelect.appendChild(currentUserOption);
                {% endif %}
                
                fetch('{{ path('app_api_users') }}')
                    .then(resp => resp.json())
                    .then(users => {
                        users.forEach(user => {
                            // Don't add the current user again (only applicable when user is logged in)
                            {% if app.user %}
                            if (user.id != '{{ app.user.id }}') {
                            {% endif %}
                                const option = document.createElement('option');
                                option.value = user.id;
                                option.textContent = user.fullName;
                                userSelect.appendChild(option);
                            {% if app.user %}
                            }
                            {% endif %}
                        });
                    })
                    .catch(error => {
                        console.error('Error loading users:', error);
                    });
                
                // Populate tags dropdown
                const tagSelect = document.getElementById('quick-task-tags');
                // Clear all options
                tagSelect.innerHTML = '';
                
                fetch('{{ path('app_api_tags') }}')
                    .then(resp => resp.json())
                    .then(tags => {
                        tags.forEach(tag => {
                            const option = document.createElement('option');
                            option.value = tag.id;
                            option.textContent = tag.name;
                            tagSelect.appendChild(option);
                        });
                    })
                    .catch(error => {
                        console.error('Error loading tags:', error);
                    });
            });
            
            // Alternative approach: Create API endpoints and populate from them
            // For now, let's create a simpler solution that loads the data when modal opens
            
            saveQuickTaskBtn.addEventListener('click', function() {
                const title = document.getElementById('quick-task-title').value.trim();
                
                if (!title) {
                    alert('Пожалуйста, введите название задачи');
                    return;
                }
                
                // Collect form data
                const formData = {
                    title: title,
                    description: document.getElementById('quick-task-description').value,
                    priority: document.getElementById('quick-task-priority').value,
                    dueDate: document.getElementById('quick-task-due-date').value,
                    category: document.getElementById('quick-task-category').value,
                    assignedUser: document.getElementById('quick-task-assigned-user').value,
                    tags: Array.from(document.getElementById('quick-task-tags').selectedOptions).map(option => option.value)
                };
                
                // Send AJAX request to create the task
                fetch('{{ path('app_task_quick_create') }}', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'X-Requested-With': 'XMLHttpRequest',
                        'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').getAttribute('content')
                    },
                    body: JSON.stringify(formData)
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Close modal
                        const modal = bootstrap.Modal.getInstance(document.getElementById('quickTaskModal'));
                        if (modal) {
                            modal.hide();
                        }
                        
                        // Reset form
                        quickTaskForm.reset();
                        
                        // Show success message
                        alert(data.message);
                        
                        // Optionally redirect to task list or show the new task
                        window.location.reload(); // Refresh the page to show the new task
                    } else {
                        alert(data.message || 'Ошибка при создании задачи');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('Произошла ошибка при создании задачи');
                });
            });
        });
    </script>
    
    <!-- Enhanced Footer -->
    <footer class="footer-enhanced">
        <div class="container">
            <div class="row">
                <!-- Company Info -->
                <div class="col-lg-4 col-md-6 footer-section">
                    <h5 class="footer-title">
                        <i class="fas fa-chart-line"></i>
                        CRM Task Management
                    </h5>
                    <p class="footer-description">
                        Современная система управления задачами и проектами для эффективной работы вашей команды. 
                        Планируйте, отслеживайте и достигайте целей вместе.
                    </p>
                    <div class="social-links">
                        <a href="#" class="social-link" aria-label="Facebook">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a href="#" class="social-link" aria-label="Twitter">
                            <i class="fab fa-twitter"></i>
                        </a>
                        <a href="#" class="social-link" aria-label="LinkedIn">
                            <i class="fab fa-linkedin-in"></i>
                        </a>
                        <a href="#" class="social-link" aria-label="GitHub">
                            <i class="fab fa-github"></i>
                        </a>
                    </div>
                </div>
                
                <!-- Quick Links -->
                <div class="col-lg-2 col-md-6 footer-section">
                    <h5 class="footer-title">
                        <i class="fas fa-link"></i>
                        Навигация
                    </h5>
                    <ul class="footer-links">
                        <li><a href="{{ path('app_dashboard') }}" class="footer-link">
                            <i class="fas fa-home"></i>Панель
                        </a></li>
                        <li><a href="{{ path('app_task_index') }}" class="footer-link">
                            <i class="fas fa-tasks"></i>Задачи
                        </a></li>
                        <li><a href="{{ path('app_kanban_board') }}" class="footer-link">
                            <i class="fas fa-columns"></i>Канбан
                        </a></li>
                        <li><a href="{{ path('app_calendar') }}" class="footer-link">
                            <i class="fas fa-calendar"></i>Календарь
                        </a></li>
                    </ul>
                </div>
                
                <!-- Analytics Links -->
                <div class="col-lg-3 col-md-6 footer-section">
                    <h5 class="footer-title">
                        <i class="fas fa-chart-bar"></i>
                        Аналитика
                    </h5>
                    <ul class="footer-links">
                        <li><a href="{{ path('app_analytics_dashboard') }}" class="footer-link">
                            <i class="fas fa-chart-pie"></i>Дашборд
                        </a></li>
                        <li><a href="{{ path('app_advanced_analytics') }}" class="footer-link">
                            <i class="fas fa-chart-area"></i>Расширенная
                        </a></li>
                        <li><a href="{{ path('app_sales_analytics') }}" class="footer-link">
                            <i class="fas fa-dollar-sign"></i>Продажи
                        </a></li>
                        {% if app.user and app.user.hasRole('ROLE_ADMIN') %}
                        <li><a href="{{ path('app_performance_index') }}" class="footer-link">
                            <i class="fas fa-tachometer-alt"></i>Производительность
                        </a></li>
                        {% endif %}
                    </ul>
                </div>
                
                <!-- Support Links -->
                <div class="col-lg-3 col-md-6 footer-section">
                    <h5 class="footer-title">
                        <i class="fas fa-life-ring"></i>
                        Поддержка
                    </h5>
                    <ul class="footer-links">
                        <li><a href="{{ path('app_about') }}" class="footer-link">
                            <i class="fas fa-info-circle"></i>О системе
                        </a></li>
                        <li><a href="#" class="footer-link">
                            <i class="fas fa-book"></i>Документация
                        </a></li>
                        <li><a href="#" class="footer-link">
                            <i class="fas fa-headset"></i>Техподдержка
                        </a></li>
                        <li><a href="#" class="footer-link">
                            <i class="fas fa-shield-alt"></i>Безопасность
                        </a></li>
                    </ul>
                </div>
            </div>
            
            <!-- Footer Bottom -->
            <div class="footer-bottom">
                <p class="footer-bottom-text">
                    © {{ "now"|date("Y") }} CRM Task Management. Все права защищены.
                </p>
                <div class="footer-bottom-links">
                    <a href="#" class="footer-bottom-link">Политика конфиденциальности</a>
                    <a href="#" class="footer-bottom-link">Условия использования</a>
                    <a href="#" class="footer-bottom-link">Лицензия</a>
                </div>
            </div>
        </div>
    </footer>
    
    <!-- Logout Confirmation Modal -->
    {% if app.user %}
    <div class="modal fade" id="logoutModal" tabindex="-1" aria-labelledby="logoutModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header border-0">
                    <h5 class="modal-title" id="logoutModalLabel">
                        <i class="fas fa-sign-out-alt me-2 text-danger"></i>Выход из системы
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body text-center py-4">
                    <div class="mb-3">
                        <i class="fas fa-question-circle fa-3x text-warning"></i>
                    </div>
                    <h6 class="mb-3">Вы уверены, что хотите выйти?</h6>
                    <p class="text-muted mb-0">
                        Вы сможете войти снова, используя свои учетные данные
                    </p>
                </div>
                <div class="modal-footer border-0 justify-content-center">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times me-2"></i>Отмена
                    </button>
                    <a href="{{ path('app_logout') }}" class="btn btn-danger">
                        <i class="fas fa-sign-out-alt me-2"></i>Да, выйти
                    </a>
                </div>
            </div>
        </div>
    </div>
    {% endif %}
    
    <script>
        // Enhanced dropdown menu behavior
        document.addEventListener('DOMContentLoaded', function() {
            // Add smooth animations to dropdowns
            const dropdowns = document.querySelectorAll('.dropdown-toggle');
            dropdowns.forEach(dropdown => {
                dropdown.addEventListener('show.bs.dropdown', function() {
                    const menu = this.nextElementSibling;
                    if (menu && menu.classList.contains('dropdown-menu')) {
                        menu.style.animation = 'slideDown 0.3s ease-out';
                    }
                });
            });
            
            // Close dropdown when clicking outside
            document.addEventListener('click', function(e) {
                if (!e.target.closest('.dropdown')) {
                    const openDropdowns = document.querySelectorAll('.dropdown-menu.show');
                    openDropdowns.forEach(menu => {
                        const dropdown = bootstrap.Dropdown.getInstance(menu.previousElementSibling);
                        if (dropdown) dropdown.hide();
                    });
                }
            });
        });
        
        // Add CSS animation
        const style = document.createElement('style');
        style.textContent = `
            @keyframes slideDown {
                from {
                    opacity: 0;
                    transform: translateY(-10px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>