{% extends 'auth_base.html.twig' %}

{% block title %}Письмо отправлено{% endblock %}

{% block stylesheets %}
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .success-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.2);
            width: 100%;
            max-width: 500px;
            overflow: hidden;
        }
        
        .success-header {
            background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
            color: white;
            padding: 3rem 2rem;
            text-align: center;
        }
        
        .success-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            animation: checkmark 0.5s ease-in-out;
        }
        
        @keyframes checkmark {
            0% { transform: scale(0); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        
        .success-title {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .success-subtitle {
            opacity: 0.9;
            font-size: 1rem;
        }
        
        .success-body {
            padding: 2.5rem;
            text-align: center;
        }
        
        .info-box {
            background: #e3f2fd;
            border: 2px solid #2196f3;
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .info-box i {
            font-size: 2rem;
            color: #2196f3;
            margin-bottom: 1rem;
        }
        
        .btn-back {
            background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
            border: none;
            color: white;
            padding: 14px 30px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 1rem;
            transition: transform 0.3s, box-shadow 0.3s;
            text-decoration: none;
            display: inline-block;
        }
        
        .btn-back:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(52, 152, 219, 0.3);
            color: white;
        }
        
        .help-text {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid #e9ecef;
            color: #6c757d;
            font-size: 0.9rem;
        }
    </style>
{% endblock %}

{% block body %}
<div class="success-card">
    <div class="success-header">
        <div class="success-icon">
            <i class="fas fa-envelope-open-text"></i>
        </div>
        <div class="success-title">Письмо отправлено!</div>
        <div class="success-subtitle">Проверьте вашу почту</div>
    </div>
    
    <div class="success-body">
        <div class="info-box">
            <i class="fas fa-info-circle"></i>
            <p class="mb-0">
                Если учётная запись с указанным email существует, на ваш адрес была отправлена ссылка для сброса пароля.
            </p>
        </div>
        
        <div class="mb-4">
            <h6 class="text-muted mb-3">Что делать дальше?</h6>
            <ol class="text-start" style="max-width: 400px; margin: 0 auto;">
                <li class="mb-2">Откройте вашу почту</li>
                <li class="mb-2">Найдите письмо от CRM системы</li>
                <li class="mb-2">Перейдите по ссылке в письме</li>
                <li class="mb-2">Создайте новый пароль</li>
            </ol>
        </div>
        
        <a href="{{ path('app_login') }}" class="btn btn-back">
            <i class="fas fa-arrow-left me-2"></i>Вернуться к входу
        </a>
        
        <div class="help-text">
            <p class="mb-1">
                <i class="fas fa-clock me-2"></i>
                <strong>Ссылка действительна 1 час</strong>
            </p>
            <p class="mb-0">
                <small>Не получили письмо? Проверьте папку "Спам"</small>
            </p>
        </div>
    </div>
</div>
{% endblock %}
