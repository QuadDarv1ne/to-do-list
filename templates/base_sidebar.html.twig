<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}CRM система{% endblock %}</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="{{ asset('css/unified-themes.css') }}">
    
    <style>
        /* Дополнительные стили для sidebar layout */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            overflow-x: hidden;
        }
        
        /* Sidebar */
        .sidebar {
            --sidebar-width: 280px;
            --sidebar-collapsed-width: 70px;
        }
        
        .sidebar {
            position: fixed;
            left: 0;
            top: 0;
            height: 100vh;
            width: var(--sidebar-width);
            transition: width 0.3s ease;
            z-index: 1000;
            display: flex;
            flex-direction: column;
        }
        
        .sidebar.collapsed {
            width: var(--sidebar-collapsed-width);
        }
        
        .sidebar-header {
            padding: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        
        .sidebar-logo {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            flex-shrink: 0;
        }
        
        .sidebar-title {
            font-size: 1.25rem;
            font-weight: 700;
            white-space: nowrap;
            opacity: 1;
            transition: opacity 0.3s;
        }
        
        .sidebar.collapsed .sidebar-title {
            opacity: 0;
            width: 0;
        }
        
        .sidebar-nav {
            flex: 1;
            padding: 1rem 0;
            overflow-y: auto;
        }
        
        .nav-item {
            margin: 0.25rem 0.75rem;
        }
        
        .nav-link {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 0.875rem 1rem;
            text-decoration: none;
            border-radius: 10px;
            transition: all 0.2s;
            position: relative;
        }
        
        .nav-link:hover {
            background: rgba(99, 102, 241, 0.1);
        }
        
        .nav-link.active {
            background: linear-gradient(135deg, rgba(99, 102, 241, 0.2), rgba(139, 92, 246, 0.2));
        }
        
        .nav-link.active::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 3px;
            height: 60%;
            border-radius: 0 3px 3px 0;
        }
        
        .nav-icon {
            width: 20px;
            text-align: center;
            flex-shrink: 0;
        }
        
        .nav-text {
            white-space: nowrap;
            opacity: 1;
            transition: opacity 0.3s;
        }
        
        .sidebar.collapsed .nav-text {
            opacity: 0;
            width: 0;
        }
        
        .sidebar-footer {
            padding: 1rem;
        }
        
        .user-profile {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem;
            border-radius: 10px;
            cursor: pointer;
            transition: background 0.2s;
        }
        
        .user-profile:hover {
            background: rgba(99, 102, 241, 0.1);
        }
        
        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            flex-shrink: 0;
        }
        
        .user-info {
            flex: 1;
            min-width: 0;
            opacity: 1;
            transition: opacity 0.3s;
        }
        
        .sidebar.collapsed .user-info {
            opacity: 0;
            width: 0;
        }
        
        .user-name {
            font-weight: 600;
            font-size: 0.875rem;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
        
        .user-role {
            font-size: 0.75rem;
        }
        
        /* Main Content */
        .main-content {
            margin-left: var(--sidebar-width);
            min-height: 100vh;
            transition: margin-left 0.3s ease;
            padding: 2rem;
        }
        
        .sidebar.collapsed ~ .main-content {
            margin-left: var(--sidebar-collapsed-width);
        }
        
        /* Top Bar */
        .top-bar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .page-title {
            font-size: 1.75rem;
            font-weight: 700;
        }
        
        .top-actions {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        
        .search-box {
            position: relative;
        }
        
        .search-input {
            padding: 0.625rem 1rem 0.625rem 2.5rem;
            width: 300px;
            transition: all 0.2s;
        }
        
        .search-input:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
        }
        
        .search-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
        }
        
        .icon-btn {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.2s;
            position: relative;
        }
        
        .icon-btn:hover {
            background: rgba(99, 102, 241, 0.1);
        }
        
        .icon-btn .badge {
            position: absolute;
            top: -5px;
            right: -5px;
            border-radius: 10px;
            padding: 0.125rem 0.375rem;
            font-size: 0.625rem;
            font-weight: 600;
        }
        
        /* Toggle Button */
        .sidebar-toggle {
            position: absolute;
            right: -15px;
            top: 80px;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 2px 8px rgba(0,0,0,0.3);
            transition: transform 0.2s;
        }
        
        .sidebar-toggle:hover {
            transform: scale(1.1);
        }
        
        .sidebar-toggle i {
            font-size: 0.75rem;
            transition: transform 0.3s;
        }
        
        .sidebar.collapsed .sidebar-toggle i {
            transform: rotate(180deg);
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }
            
            .sidebar.mobile-open {
                transform: translateX(0);
            }
            
            .main-content {
                margin-left: 0;
            }
            
            .search-input {
                width: 200px;
            }
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Sidebar -->
    <aside class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <div class="sidebar-logo">
                <i class="fas fa-chart-line"></i>
            </div>
            <div class="sidebar-title">CRM</div>
        </div>
        
        <nav class="sidebar-nav">
            <div class="nav-item">
                <a href="{{ path('app_dashboard') }}" class="nav-link {{ app.request.attributes.get('_route') == 'app_dashboard' ? 'active' : '' }}">
                    <i class="nav-icon fas fa-home"></i>
                    <span class="nav-text">Панель</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="{{ path('app_task_index') }}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_task' ? 'active' : '' }}">
                    <i class="nav-icon fas fa-tasks"></i>
                    <span class="nav-text">Задачи</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="{{ path('app_kanban_board') }}" class="nav-link {{ app.request.attributes.get('_route') == 'app_kanban_board' ? 'active' : '' }}">
                    <i class="nav-icon fas fa-columns"></i>
                    <span class="nav-text">Канбан</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="{{ path('app_calendar') }}" class="nav-link {{ app.request.attributes.get('_route') == 'app_calendar' ? 'active' : '' }}">
                    <i class="nav-icon fas fa-calendar"></i>
                    <span class="nav-text">Календарь</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="{{ path('app_analytics_dashboard') }}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_analytics' ? 'active' : '' }}">
                    <i class="nav-icon fas fa-chart-bar"></i>
                    <span class="nav-text">Аналитика</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="{{ path('app_user_index') }}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_user' ? 'active' : '' }}">
                    <i class="nav-icon fas fa-users"></i>
                    <span class="nav-text">Пользователи</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="{{ path('app_settings') }}" class="nav-link {{ app.request.attributes.get('_route') == 'app_settings' ? 'active' : '' }}">
                    <i class="nav-icon fas fa-cog"></i>
                    <span class="nav-text">Настройки</span>
                </a>
            </div>
        </nav>
        
        <div class="sidebar-footer">
            <div class="user-profile">
                <div class="user-avatar">
                    {{ app.user.fullName|slice(0, 1)|upper }}
                </div>
                <div class="user-info">
                    <div class="user-name">{{ app.user.fullName }}</div>
                    <div class="user-role">{{ 'ROLE_ADMIN' in app.user.roles ? 'Администратор' : 'Пользователь' }}</div>
                </div>
            </div>
        </div>
        
        <div class="sidebar-toggle" onclick="toggleSidebar()">
            <i class="fas fa-chevron-left"></i>
        </div>
    </aside>
    
    <!-- Main Content -->
    <main class="main-content">
        <div class="top-bar">
            <h1 class="page-title">{% block page_title %}Панель управления{% endblock %}</h1>
            
            <div class="top-actions">
                <div class="search-box">
                    <i class="search-icon fas fa-search"></i>
                    <input type="text" class="search-input" placeholder="Поиск...">
                </div>
                
                <button class="icon-btn">
                    <i class="fas fa-moon"></i>
                </button>
                
                <button class="icon-btn">
                    <i class="fas fa-bell"></i>
                    <span class="badge">3</span>
                </button>
                
                <a href="{{ path('app_profile_show') }}" class="icon-btn">
                    <i class="fas fa-user"></i>
                </a>
            </div>
        </div>
        
        {% block body %}{% endblock %}
    </main>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ asset('js/unified-theme-system.js') }}"></script>
    <script>
        function toggleSidebar() {
            document.getElementById('sidebar').classList.toggle('collapsed');
            localStorage.setItem('sidebarCollapsed', document.getElementById('sidebar').classList.contains('collapsed'));
        }
        
        // Restore sidebar state
        if (localStorage.getItem('sidebarCollapsed') === 'true') {
            document.getElementById('sidebar').classList.add('collapsed');
        }
        
        // Mobile menu toggle
        document.addEventListener('DOMContentLoaded', function() {
            const sidebar = document.getElementById('sidebar');
            
            // Close sidebar on mobile when clicking outside
            document.addEventListener('click', function(e) {
                if (window.innerWidth <= 768 && !sidebar.contains(e.target)) {
                    sidebar.classList.remove('mobile-open');
                }
            });
        });
    </script>
    
    {% block javascripts %}{% endblock %}
</body>
</html>
