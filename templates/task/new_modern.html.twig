{% extends 'base_modern.html.twig' %}

{% block title %}Создать задачу{% endblock %}

{% block body %}
<!-- Breadcrumb -->
<nav style="margin-bottom: 20px;">
    <ol style="list-style: none; padding: 0; display: flex; gap: 10px; color: var(--text-secondary); font-size: 14px;">
        <li><a href="{{ path('app_dashboard') }}" style="color: var(--text-secondary); text-decoration: none;">Панель</a></li>
        <li>/</li>
        <li><a href="{{ path('app_task_index') }}" style="color: var(--text-secondary); text-decoration: none;">Задачи</a></li>
        <li>/</li>
        <li style="color: var(--text-primary);">Создать</li>
    </ol>
</nav>

<!-- Header -->
<div style="margin-bottom: 30px;">
    <h2 style="margin: 0 0 10px 0; font-size: 32px; font-weight: 600;">Создать задачу</h2>
    <p style="color: var(--text-secondary); margin: 0;">Заполните форму для создания новой задачи</p>
</div>

<!-- Form -->
<div style="display: grid; grid-template-columns: 2fr 1fr; gap: 20px;">
    <!-- Main Form -->
    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
            
            <div style="margin-bottom: 24px;">
                <label style="display: block; color: var(--text-secondary); font-size: 13px; font-weight: 600; text-transform: uppercase; margin-bottom: 8px;">
                    Название задачи
                </label>
                {{ form_widget(form.title, {
                    'attr': {
                        'class': 'form-control',
                        'style': 'background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 16px; border-radius: 10px; font-size: 16px; width: 100%;',
                        'placeholder': 'Введите название задачи...'
                    }
                }) }}
                {{ form_errors(form.title) }}
            </div>
            
            <div style="margin-bottom: 24px;">
                <label style="display: block; color: var(--text-secondary); font-size: 13px; font-weight: 600; text-transform: uppercase; margin-bottom: 8px;">
                    Описание
                </label>
                {{ form_widget(form.description, {
                    'attr': {
                        'class': 'form-control',
                        'style': 'background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 16px; border-radius: 10px; font-size: 14px; width: 100%; min-height: 150px;',
                        'placeholder': 'Опишите задачу подробнее...',
                        'rows': 6
                    }
                }) }}
                {{ form_errors(form.description) }}
            </div>
            
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 24px;">
                <div>
                    <label style="display: block; color: var(--text-secondary); font-size: 13px; font-weight: 600; text-transform: uppercase; margin-bottom: 8px;">
                        Статус
                    </label>
                    {{ form_widget(form.status, {
                        'attr': {
                            'class': 'form-select',
                            'style': 'background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 16px; border-radius: 10px; width: 100%;'
                        }
                    }) }}
                    {{ form_errors(form.status) }}
                </div>
                
                <div>
                    <label style="display: block; color: var(--text-secondary); font-size: 13px; font-weight: 600; text-transform: uppercase; margin-bottom: 8px;">
                        Приоритет
                    </label>
                    {{ form_widget(form.priority, {
                        'attr': {
                            'class': 'form-select',
                            'style': 'background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 16px; border-radius: 10px; width: 100%;'
                        }
                    }) }}
                    {{ form_errors(form.priority) }}
                </div>
            </div>
            
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 24px;">
                <div>
                    <label style="display: block; color: var(--text-secondary); font-size: 13px; font-weight: 600; text-transform: uppercase; margin-bottom: 8px;">
                        Категория
                    </label>
                    {{ form_widget(form.category, {
                        'attr': {
                            'class': 'form-select',
                            'style': 'background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 16px; border-radius: 10px; width: 100%;'
                        }
                    }) }}
                    {{ form_errors(form.category) }}
                </div>
                
                <div>
                    <label style="display: block; color: var(--text-secondary); font-size: 13px; font-weight: 600; text-transform: uppercase; margin-bottom: 8px;">
                        Срок выполнения
                    </label>
                    {{ form_widget(form.dueDate, {
                        'attr': {
                            'class': 'form-control',
                            'style': 'background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 16px; border-radius: 10px; width: 100%;'
                        }
                    }) }}
                    {{ form_errors(form.dueDate) }}
                </div>
            </div>
            
            {% if form.assignedUser is defined %}
            <div style="margin-bottom: 24px;">
                <label style="display: block; color: var(--text-secondary); font-size: 13px; font-weight: 600; text-transform: uppercase; margin-bottom: 8px;">
                    Назначить пользователю
                </label>
                {{ form_widget(form.assignedUser, {
                    'attr': {
                        'class': 'form-select',
                        'style': 'background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 16px; border-radius: 10px; width: 100%;'
                    }
                }) }}
                {{ form_errors(form.assignedUser) }}
            </div>
            {% endif %}
            
            {% if form.tags is defined %}
            <div style="margin-bottom: 24px;">
                <label style="display: block; color: var(--text-secondary); font-size: 13px; font-weight: 600; text-transform: uppercase; margin-bottom: 8px;">
                    Теги
                </label>
                {{ form_widget(form.tags, {
                    'attr': {
                        'class': 'form-select',
                        'style': 'background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 16px; border-radius: 10px; width: 100%;',
                        'multiple': 'multiple',
                        'size': '5'
                    }
                }) }}
                <small style="color: var(--text-secondary); font-size: 12px; margin-top: 4px; display: block;">
                    Удерживайте Ctrl для выбора нескольких тегов
                </small>
                {{ form_errors(form.tags) }}
            </div>
            {% endif %}
            
            <div style="display: flex; gap: 12px; margin-top: 32px;">
                <button type="submit" class="btn" style="background: var(--color-sales); border: none; color: #000; padding: 14px 32px; border-radius: 10px; font-weight: 600; font-size: 16px;">
                    <i class="fas fa-check me-2"></i>Создать задачу
                </button>
                <a href="{{ path('app_task_index') }}" class="btn" style="background: var(--bg-secondary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 14px 32px; border-radius: 10px; font-weight: 600; font-size: 16px; text-decoration: none; display: inline-flex; align-items: center;">
                    <i class="fas fa-times me-2"></i>Отмена
                </a>
            </div>
            
            {{ form_end(form) }}
        </div>
    </div>
    
    <!-- Sidebar Tips -->
    <div>
        <div class="card" style="margin-bottom: 20px;">
            <div class="card-header">
                <h3 class="card-title">💡 Советы</h3>
            </div>
            <div class="card-body">
                <div style="display: flex; flex-direction: column; gap: 16px;">
                    <div>
                        <div style="color: var(--color-sales); font-weight: 600; margin-bottom: 4px;">
                            <i class="fas fa-lightbulb me-2"></i>Название
                        </div>
                        <p style="color: var(--text-secondary); font-size: 13px; margin: 0;">
                            Используйте краткое и понятное название
                        </p>
                    </div>
                    
                    <div>
                        <div style="color: var(--color-orders); font-weight: 600; margin-bottom: 4px;">
                            <i class="fas fa-flag me-2"></i>Приоритет
                        </div>
                        <p style="color: var(--text-secondary); font-size: 13px; margin: 0;">
                            Высокий - срочные задачи, требующие немедленного внимания
                        </p>
                    </div>
                    
                    <div>
                        <div style="color: var(--color-bonuses); font-weight: 600; margin-bottom: 4px;">
                            <i class="fas fa-calendar me-2"></i>Срок
                        </div>
                        <p style="color: var(--text-secondary); font-size: 13px; margin: 0;">
                            Установите реалистичный срок выполнения
                        </p>
                    </div>
                    
                    <div>
                        <div style="color: var(--color-clients); font-weight: 600; margin-bottom: 4px;">
                            <i class="fas fa-tags me-2"></i>Теги
                        </div>
                        <p style="color: var(--text-secondary); font-size: 13px; margin: 0;">
                            Используйте теги для быстрого поиска и фильтрации
                        </p>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">⌨️ Горячие клавиши</h3>
            </div>
            <div class="card-body">
                <div style="display: flex; flex-direction: column; gap: 12px; font-size: 13px;">
                    <div style="display: flex; justify-content: space-between;">
                        <span style="color: var(--text-secondary);">Сохранить</span>
                        <kbd style="background: var(--bg-secondary); padding: 4px 8px; border-radius: 4px; border: 1px solid var(--border-color);">Ctrl + S</kbd>
                    </div>
                    <div style="display: flex; justify-content: space-between;">
                        <span style="color: var(--text-secondary);">Отмена</span>
                        <kbd style="background: var(--bg-secondary); padding: 4px 8px; border-radius: 4px; border: 1px solid var(--border-color);">Esc</kbd>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script>
// Горячие клавиши
document.addEventListener('keydown', (e) => {
    // Ctrl + S для сохранения
    if (e.ctrlKey && e.key === 's') {
        e.preventDefault();
        document.querySelector('form').submit();
    }
    
    // Esc для отмены
    if (e.key === 'Escape') {
        window.location.href = '{{ path('app_task_index') }}';
    }
});

// Автосохранение в localStorage
const form = document.querySelector('form');
const inputs = form.querySelectorAll('input, textarea, select');

inputs.forEach(input => {
    // Загрузить сохраненные данные
    const savedValue = localStorage.getItem('task_form_' + input.name);
    if (savedValue && !input.value) {
        input.value = savedValue;
    }
    
    // Сохранять при изменении
    input.addEventListener('change', () => {
        localStorage.setItem('task_form_' + input.name, input.value);
    });
});

// Очистить localStorage после отправки
form.addEventListener('submit', () => {
    inputs.forEach(input => {
        localStorage.removeItem('task_form_' + input.name);
    });
});
</script>
{% endblock %}
