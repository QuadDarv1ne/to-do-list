{% extends 'base.html.twig' %}

{% block title %}Мой профиль{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card card-enhanced border-0 shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h3 class="card-title mb-0">
                        <i class="fas fa-user me-2"></i>Мой профиль
                    </h3>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-3 text-center">
                            <div class="mb-3">
                                <div class="rounded-circle bg-primary bg-opacity-10 d-inline-flex align-items-center justify-content-center" 
                                     style="width: 120px; height: 120px;">
                                    <i class="fas fa-user fa-3x text-primary"></i>
                                </div>
                            </div>
                            <h5 class="mt-3">{{ user.fullName }}</h5>
                            <p class="text-muted">
                                {% if user.hasRole('ROLE_ADMIN') %}
                                    <span class="badge bg-danger">Администратор</span>
                                {% else %}
                                    <span class="badge bg-info">Пользователь</span>
                                {% endif %}
                            </p>
                        </div>
                        <div class="col-md-9">
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Email:</strong></div>
                                <div class="col-sm-8">
                                    <span class="badge bg-light text-dark">{{ user.email }}</span>
                                </div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Логин:</strong></div>
                                <div class="col-sm-8">{{ user.username }}</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Имя:</strong></div>
                                <div class="col-sm-8">{{ user.firstName ?? 'Не указано' }}</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Фамилия:</strong></div>
                                <div class="col-sm-8">{{ user.lastName ?? 'Не указана' }}</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Телефон:</strong></div>
                                <div class="col-sm-8">{{ user.phone ?? 'Не указан' }}</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Должность:</strong></div>
                                <div class="col-sm-8">{{ user.position ?? 'Не указана' }}</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Отдел:</strong></div>
                                <div class="col-sm-8">{{ user.department ?? 'Не указан' }}</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Дата создания:</strong></div>
                                <div class="col-sm-8">{{ user.createdAt ? user.createdAt|date('d.m.Y H:i') : 'N/A' }}</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-4"><strong>Последний вход:</strong></div>
                                <div class="col-sm-8">{{ user.lastLoginAt ? user.lastLoginAt|date('d.m.Y H:i') : 'Еще не входил' }}</div>
                            </div>
                            <div class="row">
                                <div class="col-sm-4"><strong>Статус:</strong></div>
                                <div class="col-sm-8">
                                    {% if user.isActive %}
                                        <span class="badge bg-success">Активен</span>
                                    {% else %}
                                        <span class="badge bg-secondary">Деактивирован</span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-footer bg-white text-end border-top">
                    <a href="{{ path('app_profile_edit') }}" class="btn btn-outline-primary btn-enhanced me-2">
                        <i class="fas fa-edit me-1"></i>Редактировать
                    </a>
                    <a href="{{ path('app_profile_change_password') }}" class="btn btn-outline-warning btn-enhanced">
                        <i class="fas fa-key me-1"></i>Сменить пароль
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}