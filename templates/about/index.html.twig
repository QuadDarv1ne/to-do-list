{% extends 'base.html.twig' %}

{% block title %}О системе{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="card shadow-lg border-0">
                <div class="card-header bg-primary text-white text-center py-4">
                    <h1 class="mb-0">
                        <i class="fas fa-chart-line me-2"></i>
                        {{ system_name }}
                    </h1>
                    <p class="mb-0 mt-2 opacity-75">{{ company_name }}</p>
                </div>
                
                <div class="card-body p-5">
                    <div class="row mb-5">
                        <div class="col-md-12">
                            <h3 class="mb-4">
                                <i class="fas fa-info-circle text-primary me-2"></i>
                                О системе
                            </h3>
                            <p class="lead">
                                CRM система для анализа продаж разработана специально для 
                                {{ company_name }} с целью оптимизации процессов управления 
                                продажами и повышения эффективности работы команды.
                            </p>
                        </div>
                    </div>
                    
                    <div class="row mb-5">
                        <div class="col-md-12">
                            <h3 class="mb-4">
                                <i class="fas fa-star text-warning me-2"></i>
                                Основные возможности
                            </h3>
                            <div class="row">
                                {% for feature in features %}
                                    <div class="col-md-6 mb-3">
                                        <div class="d-flex align-items-start">
                                            <i class="fas fa-check-circle text-success me-3 mt-1"></i>
                                            <span>{{ feature }}</span>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                    
                    <div class="row mb-4">
                        <div class="col-md-4 text-center mb-4">
                            <div class="p-4 bg-light rounded">
                                <i class="fas fa-code fa-3x text-primary mb-3"></i>
                                <h5>Технологии</h5>
                                <p class="text-muted mb-0">Symfony 8.0, PHP 8.5</p>
                            </div>
                        </div>
                        <div class="col-md-4 text-center mb-4">
                            <div class="p-4 bg-light rounded">
                                <i class="fas fa-shield-alt fa-3x text-success mb-3"></i>
                                <h5>Безопасность</h5>
                                <p class="text-muted mb-0">2FA, шифрование данных</p>
                            </div>
                        </div>
                        <div class="col-md-4 text-center mb-4">
                            <div class="p-4 bg-light rounded">
                                <i class="fas fa-rocket fa-3x text-danger mb-3"></i>
                                <h5>Версия</h5>
                                <p class="text-muted mb-0">{{ version }}</p>
                            </div>
                        </div>
                    </div>
                    
                    <div class="text-center mt-5">
                        <a href="{{ path('app_dashboard') }}" class="btn btn-primary btn-lg">
                            <i class="fas fa-arrow-left me-2"></i>
                            Вернуться на главную
                        </a>
                    </div>
                </div>
                
                <div class="card-footer text-center text-muted py-3">
                    <small>
                        &copy; {{ "now"|date("Y") }} {{ company_name }}. Все права защищены.
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
