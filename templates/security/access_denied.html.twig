{% extends 'auth_base.html.twig' %}

{% block title %}Доступ запрещён{% endblock %}

{% block stylesheets %}
<style>
    body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    .access-denied-container {
        width: 100%;
        max-width: 600px;
        background: white;
        border-radius: 24px;
        box-shadow: 0 30px 80px rgba(0,0,0,0.25);
        overflow: hidden;
        animation: slideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1);
    }
    
    @keyframes slideUp {
        from {
            opacity: 0;
            transform: translateY(40px) scale(0.95);
        }
        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }
    
    .access-denied-header {
        background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
        color: white;
        padding: 3rem 2rem;
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    
    .access-denied-header::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
        animation: rotate 20s linear infinite;
    }
    
    @keyframes rotate {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }
    
    .access-denied-icon {
        font-size: 5rem;
        margin-bottom: 1rem;
        position: relative;
        z-index: 1;
        animation: shake 0.5s ease-in-out;
    }
    
    @keyframes shake {
        0%, 100% { transform: translateX(0); }
        25% { transform: translateX(-10px); }
        75% { transform: translateX(10px); }
    }
    
    .access-denied-header h1 {
        font-size: 2.5rem;
        font-weight: 700;
        margin: 0 0 0.5rem 0;
        position: relative;
        z-index: 1;
    }
    
    .access-denied-header p {
        opacity: 0.95;
        font-size: 1.1rem;
        margin: 0;
        position: relative;
        z-index: 1;
    }
    
    .access-denied-body {
        padding: 3rem 2.5rem;
        text-align: center;
    }
    
    .error-code {
        font-size: 6rem;
        font-weight: 900;
        color: #e74c3c;
        line-height: 1;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
    }
    
    .error-message {
        font-size: 1.2rem;
        color: #2c3e50;
        margin-bottom: 2rem;
    }
    
    .info-box {
        background: #f8f9fa;
        border-left: 4px solid #e74c3c;
        padding: 1.5rem;
        border-radius: 8px;
        margin-bottom: 2rem;
        text-align: left;
    }
    
    .info-box ul {
        margin: 0;
        padding-left: 1.5rem;
    }
    
    .info-box li {
        margin-bottom: 0.5rem;
    }
    
    .btn-group-custom {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }
    
    .btn-custom {
        padding: 0.875rem 2rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .btn-primary-custom {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
    }
    
    .btn-primary-custom:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        color: white;
    }
    
    .btn-secondary-custom {
        background: #6c757d;
        color: white;
        border: none;
    }
    
    .btn-secondary-custom:hover {
        background: #5a6268;
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(108, 117, 125, 0.4);
        color: white;
    }
</style>
{% endblock %}

{% block body %}
<div class="access-denied-container">
    <div class="access-denied-header">
        <div class="access-denied-icon">
            <i class="fas fa-ban"></i>
        </div>
        <h1>Доступ запрещён</h1>
        <p>У вас нет прав для просмотра этой страницы</p>
    </div>
    
    <div class="access-denied-body">
        <div class="error-code">403</div>
        <div class="error-message">
            К сожалению, у вас недостаточно прав для доступа к запрашиваемому ресурсу
        </div>
        
        <div class="info-box">
            <strong><i class="fas fa-info-circle me-2"></i>Возможные причины:</strong>
            <ul class="mt-2">
                <li>У вас нет необходимых прав доступа</li>
                <li>Требуется роль администратора или менеджера</li>
                <li>Ресурс доступен только определённым пользователям</li>
                <li>Ваша сессия могла истечь</li>
            </ul>
        </div>
        
        <div class="btn-group-custom">
            {% if app.user %}
                <a href="{{ path('app_dashboard') }}" class="btn-custom btn-primary-custom">
                    <i class="fas fa-home"></i>
                    На главную
                </a>
                <a href="javascript:history.back()" class="btn-custom btn-secondary-custom">
                    <i class="fas fa-arrow-left"></i>
                    Назад
                </a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn-custom btn-primary-custom">
                    <i class="fas fa-sign-in-alt"></i>
                    Войти в систему
                </a>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
